(()=>{
const form=document.querySelector('#chatForm');if(!form)return;
const box=document.querySelector('#messages'),input=document.querySelector('#message'),typing=document.querySelector('#typing'),subject=document.querySelector('#subject'),lesson=document.querySelector('#lesson'),send=form.querySelector('button[type="submit"]'),title=document.querySelector('#conversationTitle'),meta=document.querySelector('#conversationMeta'),counter=document.querySelector('#charCount'),side=document.querySelector('.tutor-side'),contextToggle=document.querySelector('#mobileContextToggle'),mobileClear=document.querySelector('#mobileClear'),quota=document.querySelector('#aiQuota'),remaining=document.querySelector('#aiRemaining');subject.required=false;lesson.required=false;
const updateQuota=value=>{if(value===null||value===undefined||!remaining)return;const left=Math.max(0,Number(value)||0);remaining.textContent=String(left);quota?.classList.toggle('empty',left===0);quota?.setAttribute('aria-label',left+' of 10 free AI responses remaining today')};updateQuota(EDU.remaining);
const inline=s=>s.replace(/\*\*(.+?)\*\*/g,'<strong>$1</strong>').replace(/\[Textbook page ([^\]]+)\]/gi,'<span class="page-ref">📖 Page $1</span>');
const format=t=>{const escape=document.createElement('div');escape.textContent=t;const lines=escape.innerHTML.split(/\r?\n/);let html='',list='';const close=()=>{if(list){html+=`</${list}>`;list=''}};for(const raw of lines){const line=raw.trim();if(!line){close();continue}const heading=line.match(/^(#{2,4})\s+(.+)/);if(heading){close();const level=heading[1].length;html+=`<h${level}>${inline(heading[2])}</h${level}>`;continue}const bullet=line.match(/^[-*•]\s+(.+)/);if(bullet){if(list!=='ul'){close();list='ul';html+='<ul>'}html+=`<li>${inline(bullet[1])}</li>`;continue}const numbered=line.match(/^\d+[.)]\s+(.+)/);if(numbered){if(list!=='ol'){close();list='ol';html+='<ol>'}html+=`<li>${inline(numbered[1])}</li>`;continue}close();html+=`<p>${inline(line)}</p>`}close();return html};
const removeWelcome=()=>document.querySelector('#chatWelcome')?.remove();const welcome=()=>{if(!document.querySelector('#chatWelcome'))box.innerHTML='<div class="chat-welcome" id="chatWelcome"><div class="welcome-orb">✨</div><h2>What would you like to learn?</h2><p>Ask any educational question, or select a lesson for textbook-based help.</p></div>'};
const add=(role,text)=>{removeWelcome();const row=document.createElement('div'),bubble=document.createElement('div'),avatar=document.createElement('div');row.className='message-row '+role;bubble.className='chat-message';role==='assistant'?bubble.innerHTML=format(text):bubble.textContent=text;avatar.className='message-avatar';avatar.textContent=role==='assistant'?'🎓':'👤';role==='assistant'?row.append(avatar,bubble):row.append(bubble,avatar);box.append(row);if(role==='assistant'&&window.MathJax?.typesetPromise)window.MathJax.typesetPromise([bubble]).catch(()=>{});box.scrollTop=box.scrollHeight};
fetch(EDU.history).then(r=>r.json()).then(items=>items.forEach(m=>add(m.role,m.message))).catch(()=>{});
const placeholder=lesson.options[0].cloneNode(true),allLessons=[...lesson.options].slice(1).map(o=>o.cloneNode(true));
const updateContext=()=>{const selected=lesson.selectedOptions[0];title.textContent=selected&&lesson.value?selected.textContent.trim():'General education chat';meta.textContent=subject.value&&lesson.value?'Textbook connected · Ask a question':'Ask any education-related question'};
const loadLessons=()=>{const matching=allLessons.filter(o=>o.dataset.subject===subject.value).map(o=>o.cloneNode(true));lesson.replaceChildren(placeholder.cloneNode(true),...matching);lesson.disabled=!subject.value;lesson.value='';updateContext()};subject.addEventListener('change',loadLessons);lesson.addEventListener('change',()=>{updateContext();if(matchMedia('(max-width:800px)').matches&&lesson.value){side.classList.remove('context-open');contextToggle?.setAttribute('aria-expanded','false')}});loadLessons();
contextToggle?.addEventListener('click',()=>{const open=side.classList.toggle('context-open');contextToggle.setAttribute('aria-expanded',String(open))});const resize=()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,140)+'px';counter.textContent=`${input.value.length} / 1000`};input.addEventListener('input',resize);resize();
const submit=async()=>{const text=input.value.trim(),selected=lesson.selectedOptions[0];if(!text)return;add('user',text);input.value='';resize();typing.hidden=false;send.disabled=true;try{const response=await fetch(EDU.send,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({csrf:EDU.csrf,message:text,subject_id:subject.value||null,lesson_id:selected?.dataset.lessonId||null,textbook_subject:selected?.dataset.textbookSubject||null,textbook_lesson:selected?.dataset.textbookLesson||null})}),json=await response.json();updateQuota(json.remaining);add('assistant',json.answer||json.error||'Unable to respond.')}catch{add('assistant','The tutor is temporarily unavailable. Please try again.')}finally{typing.hidden=true;send.disabled=false;input.focus()}};
form.addEventListener('submit',e=>{e.preventDefault();submit()});input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();submit()}});document.querySelectorAll('[data-prompt]').forEach(b=>b.addEventListener('click',()=>{input.value=b.dataset.prompt;resize();submit()}));
const clear=async()=>{if(!confirm('Clear chat history?'))return;await fetch(EDU.history,{method:'DELETE',headers:{'X-CSRF-Token':EDU.csrf}});box.textContent='';welcome();input.focus()};document.querySelector('#clearChat')?.addEventListener('click',clear);mobileClear?.addEventListener('click',clear);
})();
