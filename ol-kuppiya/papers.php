<?php
require_once __DIR__.'/../includes/auth.php';require_once __DIR__.'/../includes/ol_schema.php';require_login();require_premium();if(!in_array(user_grade_number(),[10,11],true)){redirect('student/dashboard.php');}ensure_ol_schema();$sid=filter_input(INPUT_GET,'subject_id',FILTER_VALIDATE_INT);$medium=medium_language((string)user()['medium']);$subject=$sid?query_one('SELECT * FROM subjects WHERE id=? AND status="active"','i',[$sid]):null;if(!$subject){redirect('ol-kuppiya/subjects.php');}$s=db()->prepare('SELECT year,COUNT(*) total FROM ol_papers WHERE subject_id=? AND medium=? AND status=1 GROUP BY year ORDER BY year DESC');$s->bind_param('is',$sid,$medium);$s->execute();$years=$s->get_result()->fetch_all(MYSQLI_ASSOC);$pageTitle='O/L papers';include __DIR__.'/../includes/header.php';?>
<p><a href="subjects.php">← Subjects</a></p><h1><?=e(locale_value($subject,'name'))?> past papers</h1><div class="grid"><?php foreach($years as $y):?><a class="card" style="text-decoration:none;color:inherit" href="paper.php?subject_id=<?=$sid?>&amp;year=<?=(int)$y['year']?>"><h2><?=intval($y['year'])?></h2><p class="muted">View paper and answer sheet →</p></a><?php endforeach;?></div>
<?php include __DIR__.'/../includes/footer.php';
